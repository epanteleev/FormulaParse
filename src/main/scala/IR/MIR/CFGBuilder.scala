package IR.MIR

class CFGBuilder(val cfg: Graph) {
  //  var blocks:List[Block] = _
  //  var edges: Edges = _
  //
  //  def addInstruction(codeOp: List[CodeOp]): Unit = {
  //    blocks = blocks ++ List(NonEmpty(codeOp))
  //  }
}
